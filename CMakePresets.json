{
    "version": 3,
    "configurePresets": [
        {
            "name": "default",
            "binaryDir": "build/robot",
            "displayName": "Default",
            "description": "Default ESP-IDF build configuration.",
            "cacheVariables": {
                "SDKCONFIG_DEFAULTS": "sdkconfig.defaults",
                "SDKCONFIG": "./build/robot/sdkconfig"
            }
        }
    ]
}